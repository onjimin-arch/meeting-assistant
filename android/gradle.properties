org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Flutter 3.44.0 + AGP 9+ 조합: 구형 DSL 플러그인(record_android 등) 호환을 위해 compat 모드 사용.
# Flutter Fix 권장사항 "opt out of android.newDsl" 적용.
android.newDsl=false
