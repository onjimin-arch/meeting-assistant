# Meeting Assistant 앱 설계서
> 온디바이스 AI 기반 회의 자동 기록 & 노션 저장 모바일 앱

---

## Executive Summary

> **핵심 주장**: Whisper Tiny + Gemma 4 2B 온디바이스 파이프라인으로 회의 녹음 → 텍스트 변환 → 회의록 생성 → Notion 저장을 완전 자동화하는 Flutter 모바일 앱을 구현한다.

- **주요 근거**: 온디바이스 처리로 외부 서버 의존 없이 작동. Gemma 4 2B (~1.5GB) + Whisper Tiny (~39MB) 조합으로 RAM 6GB 이상 중고급 기기에서 실용적 품질 확보 가능
- **결론**: Claude Code에서 단일 오케스트레이터 + 2개 서브에이전트 구조로 구현하며, 스킬 4종이 핵심 AI·저장 기능을 담당한다

---

## 1. 작업 컨텍스트

> **핵심**: 회의 참석자가 별도 조작 없이 녹음 버튼 하나로 회의록을 자동 생성·저장할 수 있는 온디바이스 AI 앱이다.

### 1.1 배경 및 목적

| 항목 | 내용 |
|------|------|
| **목적** | 회의 녹음 → STT → 회의록 생성 → Notion 저장 완전 자동화 |
| **대상 사용자** | 바로고 내부 및 지인 배포 (APK/IPA 파일 직접 전달) |
| **제약** | 외부 서버 없음, 완전 온디바이스 처리, 앱스토어 미출시 |

### 1.2 입출력 정의

**입력**
- 회의 음성 (마이크 실시간 녹음)
- 사용자 설정: Notion API 토큰, 저장 페이지 URL, 회의록 작성 지침
- 추가 작업 요청: 자유 프롬프트 / 프리셋 (액션아이템 추출, 영문 요약, 임원 보고용 재작성)

**출력**
- 원본 스크립트 (Whisper 변환 텍스트) — 로컬 저장 + 앱 내 열람/복사
- 회의록 (Gemma 생성 구조화 마크다운) — 로컬 저장 + Notion 페이지 저장
- 추가 작업 결과 — 인라인 채팅 응답

### 1.3 제약조건

| 구분 | 내용 |
|------|------|
| **모델 크기** | Gemma 4 2B (~1.5GB), Whisper Tiny (~39MB) — 앱 번들 미포함, 첫 실행 시 다운로드 |
| **디바이스 최소 사양** | RAM 6GB 이상 권장 (Gemma 4 2B 실행 기준) |
| **네트워크** | 모델 다운로드·Notion 저장 시에만 필요. 녹음·변환·생성은 오프라인 가능 |
| **플랫폼** | Android (APK), iOS (IPA) — Flutter 크로스플랫폼 |
| **배포** | 파일 직접 전달 방식 (앱스토어 미사용) |
| **Notion 인증** | 사용자 개인 Integration Token 방식 (OAuth 미적용) |

### 1.4 용어 정의

| 용어 | 정의 |
|------|------|
| **STT** | Speech-to-Text. Whisper Tiny가 오디오 → 텍스트 변환 수행 |
| **원본 스크립트** | Whisper가 생성한 날 텍스트 (화자 구분 없는 연속 텍스트) |
| **회의록** | Gemma 4 2B가 사용자 지침에 따라 구조화한 최종 문서 |
| **추가 작업** | 회의록 생성 후 저장된 텍스트 기반 후속 작업 (Q&A, 재작성 등) |
| **온디바이스** | 기기 내 모델 실행. 인터넷 없이 추론 가능 |

---

## 2. 워크플로우 정의

> **핵심**: 메인 녹음·생성 플로우와 추가 작업 플로우 2개로 구성되며, LLM 판단 영역과 코드 처리 영역이 명확히 분리된다.

### 2.1 전체 플로우 개요

```
[홈 화면]
    │
    ├─ 새 회의 녹음 ──► [녹음 중] ──► [처리 중] ──► [회의록 화면]
    │                                                      │
    │                                          ┌───────────┴───────────┐
    │                                    [원본 스크립트 보기]    [추가 작업 채팅]
    │
    ├─ 기존 회의 선택 ────────────────────────────► [회의록 화면]
    │
    └─ 설정 ──► [Notion 연동 / 작성 지침 / 모델 정보]
```

### 2.2 메인 플로우: 녹음 → 회의록 생성

#### Step 1. 녹음 시작·진행
- **처리 방식**: 코드 (flutter_sound)
- **UI 구성**: 상단 REC 뱃지 → 중단 실시간 웨이브폼 → 하단 녹음 타이머 + 종료 버튼 + "탭하여 종료" 텍스트
- **성공 기준**: 오디오 스트림 정상 수신, 타이머 동작
- **검증 방법**: 규칙 기반 — 마이크 권한 허용 여부 확인
- **실패 처리**: 권한 없음 → 에스컬레이션 (권한 요청 다이얼로그)

#### Step 2. 녹음 종료 → 오디오 파일 저장
- **처리 방식**: 코드 (로컬 파일시스템 저장, `.m4a` / `.wav`)
- **성공 기준**: 파일 크기 > 0, 재생 가능 포맷
- **검증 방법**: 스키마 검증 — 파일 존재 여부 + 최소 크기 체크
- **실패 처리**: 저장 실패 → 자동 재시도 1회, 이후 에스컬레이션

#### Step 3. STT 변환 (Whisper Tiny)
- **처리 방식**: 코드 (whisper.cpp FFI 호출)
- **입력**: 오디오 파일 경로
- **출력**: 원본 스크립트 텍스트
- **성공 기준**: 텍스트 길이 > 0, 최소 50자 이상
- **검증 방법**: 규칙 기반 — 빈 문자열 여부, 최소 글자 수
- **실패 처리**: 실패 시 재시도 1회 → 이후 스킵 + 로그 (빈 스크립트로 다음 단계 진행)

#### Step 4. 회의록 생성 + 제목 생성 (Gemma 4 2B) ← **LLM 판단 영역**
- **처리 방식**: LLM (Gemma 4 2B on-device)
- **입력**: 원본 스크립트 + 사용자 작성 지침
- **출력**: 회의 제목 + 구조화된 회의록 마크다운 (JSON 단일 응답)
- **프롬프트 구조**:
  ```
  [시스템] 사용자 지침 + 기본 형식 (일시, 참석자, 안건, 결정사항, 액션아이템)
           + 제목 포함 JSON 반환 지시
  [유저] 원본 스크립트 전문
  ```
- **응답 형식**: `{"title": "Q2 전략 방향 확정", "minutes": "## 회의록\n..."}`
- **성공 기준**: title 3~20자, 필수 섹션(안건 또는 결정사항) 1개 이상, 회의록 500자 이상
- **검증 방법**: LLM 자기 검증 + 규칙 기반 (JSON 파싱 성공, 필수 필드 존재 여부)
- **실패 처리**: 검증 실패 → 자동 재생성 1회, 이후 에스컬레이션. 제목 파싱 실패 시 `날짜_회의` 포맷으로 폴백

#### Step 5. 로컬 저장 (SQLite)
- **처리 방식**: 코드 (drift ORM)
- **저장 항목**: 회의 제목(날짜+시간 자동 생성), 일시, 원본 스크립트, 회의록, 오디오 경로
- **성공 기준**: DB insert 오류 없음
- **검증 방법**: 스키마 검증 — insert 결과 ID 확인
- **실패 처리**: 자동 재시도 2회, 이후 에스컬레이션

#### Step 6. Notion 저장 (조건부)
- **처리 방식**: 코드 (Notion REST API v1)
- **실행 조건**: 자동 저장 ON → 회의록 생성 완료 직후 자동 실행 / 자동 저장 OFF → 사용자가 "Notion 저장" 버튼 탭 시 실행
- **처리 화면 표시**: 자동 저장 ON일 때만 Processing 화면에 "Notion에 저장 중" 단계 노출
- **입력**: 회의록 마크다운, 사용자 설정 페이지 URL + API 토큰
- **출력**: Notion 페이지 생성 완료 (페이지 ID 반환)
- **성공 기준**: API 응답 200
- **검증 방법**: 스키마 검증 — HTTP 상태 코드 확인
- **실패 처리**: 네트워크 오류 → 스킵 + 로그 (로컬엔 저장됨, 수동 재시도 버튼 제공)

---

### 2.3 추가 작업 플로우 (채팅)

회의록 생성 완료 후 진입. 원본 스크립트 + 회의록 전문을 컨텍스트로 포함.

| 작업 유형 | LLM 처리 내용 | 성공 기준 |
|-----------|-------------|----------|
| **액션아이템 추출** | 담당자·마감일 포함 리스트 형태 추출 | 항목 1개 이상 포함 |
| **영문 요약** | 핵심 내용 영문 요약문 생성 | 영문 텍스트 100자 이상 |
| **임원 보고용 재작성** | 3~5줄 경영진 요약 형식 | 불릿 또는 단락 형식 |
| **자유 프롬프트** | 컨텍스트 기반 Q&A, 임의 분석 | 응답 존재 여부 |

**실패 처리**: LLM 응답 없음 → 스킵 + "다시 시도" 버튼 표시

---

### 2.4 LLM 판단 vs 코드 처리 분리표

| LLM (Gemma 4 2B)이 직접 수행 | 코드(스크립트)로 처리 |
|-------------------------------|----------------------|
| 회의록 구조화 및 생성 | 오디오 녹음·저장 (flutter_sound) |
| 회의 제목 자동 생성 (회의록과 동일 호출) | STT 호출 (Whisper FFI) |
| 액션아이템 추출 및 분류 | SQLite CRUD (drift) |
| 컨텍스트 기반 Q&A | Notion API 호출 |
| 영문 요약, 형식 재작성 | 파일 I/O, 모델 다운로드 관리 |

---

## 3. 구현 스펙

> **핵심**: Flutter 앱 + Claude Code 오케스트레이터 구조로 구현하며, AI 파이프라인과 Notion 연동은 전문성이 다르므로 서브에이전트로 분리한다.

### 3.1 기술 스택

| 영역 | 선택 | 근거 |
|------|------|------|
| **크로스플랫폼** | Flutter (Dart) | Android APK + iOS IPA 단일 코드베이스 |
| **오디오 녹음** | flutter_sound | Flutter 생태계 표준, 포맷 유연성 |
| **STT** | whisper_flutter_new 패키지 | 직접 FFI 대비 유지보수 부담 낮음 |
| **LLM** | MediaPipe LLM Inference API | Google 공식 Gemma 지원, Android/iOS 통합 |
| **로컬 DB** | drift (SQLite ORM) | 타입 안전, Flutter 표준 |
| **Notion 연동** | Notion REST API v1 | 공식 API, Personal Integration Token |
| **상태 관리** | Riverpod | 비동기 상태 처리, Flutter 표준 |

> **전역 상태 관리 주요 항목**
> - `autoSave` (bool): 자동 저장 설정값. 설정 화면·Processing 화면·회의록 화면이 공유. Riverpod SharedPreferences Provider로 영속화
> - `recordingState`: 녹음 중·처리 중·완료 상태 전이 관리
> - `currentMeeting`: 현재 열람 중인 회의 데이터 (스크립트·회의록·메타데이터)

### 3.2 폴더 구조

```
/meeting-assistant
  ├── CLAUDE.md                          # 메인 오케스트레이터 지침
  ├── /.claude
  │   ├── /skills
  │   │   ├── /audio-recorder
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts               # flutter_sound 래퍼
  │   │   ├── /whisper-stt
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts               # whisper_flutter_new 연동
  │   │   │   └── /references            # 모델 설정, 언어 코드 가이드
  │   │   ├── /gemma-inference
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts               # MediaPipe LLM Inference 래퍼
  │   │   │   └── /references            # 프롬프트 템플릿, 시스템 지침
  │   │   └── /notion-sync
  │   │       ├── SKILL.md
  │   │       ├── /scripts               # Notion API 클라이언트
  │   │       └── /references            # Notion API 문서, 블록 타입 가이드
  │   └── /agents
  │       ├── /ai-pipeline
  │       │   └── AGENT.md
  │       └── /notion-integration
  │           └── AGENT.md
  ├── /lib                               # Flutter 소스
  │   ├── /screens                       # UI 화면 (Home, Recording, Minutes, Chat, Settings)
  │   ├── /services                      # AI·DB·API 서비스 레이어
  │   ├── /models                        # Meeting, Transcript, Settings 데이터 모델
  │   └── /providers                     # Riverpod 상태 관리
  ├── /assets
  │   └── /models                        # 모델 다운로드 후 저장 경로
  ├── /output                            # Claude Code 중간 산출물
  └── /docs                              # 참고 문서
```

### 3.3 CLAUDE.md 핵심 섹션 목록

| 섹션 | 내용 |
|------|------|
| 프로젝트 개요 | 앱 목적, 기술 스택, 배포 방식 요약 |
| 구현 순서 | 스캐폴딩 → AI 파이프라인 → UI 화면 → Notion 연동 → 통합 테스트 |
| 서브에이전트 호출 조건 | ai-pipeline / notion-integration 트리거 시점 명시 |
| 스킬 사용 지침 | 각 스킬 호출 시점 및 입출력 데이터 전달 방식 |
| 금지 사항 | 서브에이전트 간 직접 호출 금지, API 키 하드코딩 금지 |
| 테스트 전략 | 모델 로딩·STT 정확도·Notion API 연동 검증 방법 |

### 3.4 에이전트 구조

**단일 오케스트레이터 + 2개 서브에이전트**

```
CLAUDE.md (오케스트레이터)
    │
    ├── ai-pipeline AGENT.md
    │     Whisper + Gemma Flutter 네이티브 통합 전담
    │     (패키지 설정, 모델 로딩, 추론 서비스 구현)
    │
    └── notion-integration AGENT.md
          Notion API 클라이언트 구현 전담
          (인증, 페이지 생성, 마크다운 → Notion 블록 변환)
```

> [분석] 서브에이전트 분리 근거: AI 파이프라인(네이티브 라이브러리, FFI)과 Notion 연동(외부 REST API, 블록 포맷)은 독립적인 도메인 지식이 필요하며, 각각 지침이 길어질 경우 컨텍스트 효율화를 위해 분리가 유효하다.

### 3.5 스킬 목록

| 스킬명 | 역할 | 트리거 조건 |
|--------|------|------------|
| `audio-recorder` | 마이크 녹음 시작/종료, 오디오 파일 저장 | 녹음 버튼 탭 시 |
| `whisper-stt` | Whisper Tiny 호출, 오디오 → 텍스트 변환 | 녹음 종료 후 Processing 진입 시 |
| `gemma-inference` | Gemma 4 2B 추론, 회의록 생성 및 채팅 응답 | STT 완료 후 / 채팅 메시지 전송 시 |
| `notion-sync` | Notion REST API 호출, 회의록 페이지 생성 | 회의록 생성 완료 후 / 수동 저장 버튼 탭 시 |

### 3.6 서브에이전트 정의

#### ai-pipeline

| 항목 | 내용 |
|------|------|
| **역할** | Flutter 앱 내 Whisper + Gemma 온디바이스 추론 파이프라인 구현 |
| **트리거** | 오케스트레이터가 AI 네이티브 통합 구현 단계 진입 시 |
| **입력** | 오디오 파일 경로, 사용자 지침 텍스트, 프롬프트 템플릿 |
| **출력** | `whisper_service.dart`, `gemma_service.dart` |
| **참조 스킬** | `whisper-stt`, `gemma-inference` |
| **데이터 전달** | 파일 기반 (`/output/ai_pipeline_spec.md`) |

#### notion-integration

| 항목 | 내용 |
|------|------|
| **역할** | Notion REST API 클라이언트 및 마크다운 → Notion 블록 변환기 구현 |
| **트리거** | 오케스트레이터가 Notion 연동 구현 단계 진입 시 |
| **입력** | API 토큰 구조, 페이지 URL 파싱 방식, 회의록 마크다운 샘플 |
| **출력** | `notion_service.dart`, `block_converter.dart` |
| **참조 스킬** | `notion-sync` |
| **데이터 전달** | 파일 기반 (`/output/notion_spec.md`) |

### 3.7 주요 산출물 파일 형식

| 산출물 | 형식 | 저장 위치 |
|--------|------|----------|
| 오디오 녹음 | `.m4a` (Android) / `.caf` (iOS) | 기기 로컬 파일시스템 |
| 원본 스크립트 | TEXT 컬럼 | SQLite (drift) `meetings` 테이블 |
| 회의록 | TEXT 컬럼 (마크다운) | SQLite + Notion 페이지 |
| 회의 메타데이터 | SQLite 레코드 | `meetings` 테이블 |
| Whisper 모델 | `.bin` | 앱 Documents 디렉토리 |
| Gemma 모델 | `.task` (MediaPipe) | 앱 Documents 디렉토리 |

### 3.8 첫 실행 모델 다운로드 전략

모델 파일은 앱 번들 미포함, 첫 실행 시 다운로드.

| 모델 | 크기 | 출처 |
|------|------|------|
| Whisper Tiny | ~39MB | HuggingFace 공개 모델 |
| Gemma 4 2B (MediaPipe) | ~1.5GB | Google MediaPipe 공식 배포 |

- Wi-Fi 연결 확인 후 다운로드 권고 알림 표시
- 진행률 UI 필수 (Progress bar + 퍼센트)
- 다운로드 실패 시 재시도 버튼 제공

---

## 4. 확정 사항 (구현 반영)

> **핵심**: 미결 사항 2개 모두 확정. Notion 저장은 수동 기본 + 자동 저장 설정 옵션, 회의 제목은 Gemma 자동 생성.

### 4.1 Notion 저장 시점 — 확정

- **기본값**: 수동 저장 (회의록 화면의 "Notion 저장" 버튼 탭 시)
- **옵션**: 설정 화면에 "회의록 완성 시 자동 저장" 토글 추가
  - ON: 회의록 생성 완료 직후 자동으로 Notion API 호출
  - OFF (기본): 사용자가 검토 후 수동으로 저장

**설정 화면 스펙 추가**

```
설정 > Notion 연동
  ├── API 토큰              (text input)
  ├── 저장 페이지 URL        (text input)
  └── 회의록 완성 시 자동 저장 (toggle, 기본값: OFF)
```

**Step 6 분기 흐름**

```
[회의록 생성 완료]
        │
        ├─ 자동 저장 ON  ──► Notion API 자동 호출 ──► 토스트 알림 (성공/실패)
        │
        └─ 자동 저장 OFF ──► 회의록 화면 표시 (수동 "Notion 저장" 버튼 노출)
                                      │
                                      └─ 버튼 탭 ──► Notion API 호출
```

### 4.2 회의 제목 자동 생성 — 확정

- **방식**: Gemma 4 2B가 원본 스크립트 앞부분(최대 500자)을 기반으로 3~10자 한국어 제목 생성
- **처리 시점**: Step 4 회의록 생성과 동일 추론 호출에 병합 (별도 추론 호출 없음)
- **프롬프트 추가 지침**:
  ```
  회의록과 함께 회의 제목을 JSON으로 반환할 것.
  {"title": "Q2 전략 방향 확정", "minutes": "## 회의록\n..."}
  ```
- **폴백**: Gemma 생성 실패 또는 형식 오류 시 `날짜_회의` 포맷으로 대체 (예: `250513_회의`)
- **검증**: 제목 길이 3~20자, 특수문자 제외 — 규칙 기반 검증
